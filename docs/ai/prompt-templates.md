# Prompt Templates

The default decomposition template turns a rough request into decision-complete tasks. It instructs the planning agent to read project rules first, separate dependencies, preserve scope, define acceptance criteria, include verification, and respect the project's daily-log convention.

Generated tasks are always drafts. Users can edit, reorder, remove, or change dependencies before publishing them to the board.
