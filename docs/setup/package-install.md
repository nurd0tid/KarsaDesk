# Package Install

Use pnpm.

```bash
pnpm add @tanstack/react-query @tanstack/react-table zod react-hook-form @hookform/resolvers zustand sonner sweetalert2 lucide-react framer-motion monaco-editor @monaco-editor/react xterm xterm-addon-fit react-resizable-panels date-fns clsx tailwind-merge class-variance-authority
pnpm dlx shadcn@latest init
pnpm dlx shadcn@latest add button card dialog drawer sheet tabs input textarea select dropdown-menu command popover badge table skeleton separator tooltip alert checkbox form calendar scroll-area toast
```

Use Context7 before finalizing exact package usage.
